#!/bin/basht
## usefullTerminalLinux - version 0.0.1 written by xMeise 02/2021



fd    -->  An alternative to `find`.
           Aims to be faster and easier to use than `find`.
tldr  -->  Displays simple help pages for command-line tools, from the tldr-pages project.
exa   -->  Ein moderner Ersatz für `ls` (Verzeichnisinhalte auflisten).
bat   -->  Ausgabe und Verkettung von einzelnen Dateien.
           Ein `cat`-Ersatz mit Syntax-Hervorhebung und Git-Integration.
rg    -->  Ripgrep is a recursive line-oriented CLI search tool.
           Aims to be a faster alternative to `grep`.
tokei -->  A program that prints out statistics about code.

